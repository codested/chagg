---
type: feature
---

Add release command options for dry-run, version-only output, and optional tag push with git write policy controls. Add
config support for .chagg.yml/chagg.yml plus global/module gitWrite and autoAddToGit defaults. Enable generate output in
Markdown, JSON, and HTML formats, and make chagg add stage created files by default with opt-out flags.
