---
type: feature
---

chagg is a Git-native release management CLI that collects structured change entries in .changes, validates them,
previews staged and tagged release notes, generates changelog markdown, and creates module-aware SemVer tags for
releases.
